
server: { port: 8096 }
spring:
  application: { name: medical-record-service }
  datasource:
    url: jdbc:mysql://mysql:3306/records_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: amal
    password: amal
  jpa: { hibernate: { ddl-auto: validate } }
  flyway: { enabled: true, locations: classpath:db/migration }
eureka:
  client: { serviceUrl: { defaultZone: http://discovery-server:8761/eureka } }
  instance: { prefer-ip-address: true }
